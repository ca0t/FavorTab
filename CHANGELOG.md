# FavorTab 更新日志

## v1.1.0 (2026-08-07)

### 新增：书签卡片拖拽排序
- 引入 SortableJS，卡片支持丝滑拖拽（150ms 位移动画，拖起补位、划过挤开、放下落位）
- 同文件夹内拖到卡片前后 → 精确插入排序
- 跨文件夹拖到卡片前后 → 精确插入；拖到文件夹标题/空白区域 → 放到该文件夹第一个位置
- 所有拖放操作同步联动浏览器收藏夹（chrome.bookmarks.move），edge://bookmarks 中顺序一致
- 拖拽期间禁用 hover 发白/放大等误触反馈，松手后移动鼠标才恢复正常交互

### 新增：书签卡片重命名
- 卡片 hover 显示铅笔按钮，点击进入内联编辑（与文件夹重命名交互一致）
- Enter / 点击空白保存，Escape 取消；名称修改同步到浏览器收藏夹
- 保留用户原始输入（不 trim），输入空格则保存空格名称

### 优化：代码结构按功能拆分
- JS 拆分为 8 个模块（js/）：搜索、背景、设置、favicon、书签渲染、拖拽、交互事件、入口
- CSS 拆分为 4 个文件（css/）：基础、UI 组件、书签、响应式
- 便于后续维护与功能迭代，功能行为不变
